services:
  llama-server:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    container_name: llama-cpp-server
    restart: unless-stopped
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - ${MODELS_PATH:-C:/Users/Tobias/.cache/llama.cpp/models}:/models:ro
    ports:
      - "${PORT:-8080}:8080"
    command:
      - -m
      - ${MODEL:-/models/Qwen_Qwen3.5-35B-A3B-Q4_K_M.gguf}
      - --port
      - "8080"
      - --host
      - "0.0.0.0"
      # GPU offloading (12GB VRAM: 20 layers optimal, 15 conservative)
      - --n-gpu-layers
      - "${N_GPU_LAYERS:-20}"
      # Context window (48K recommended for coding)
      - -c
      - "${CTX_SIZE:-49152}"
      # Batch size - reduced to 256 to avoid KV cache pressure
      - -b
      - "${BATCH_SIZE:-256}"
      # Parallel slots - limit to 2 for stability with large context
      - -np
      - "${N_PARALLEL:-2}"
      # KV cache quantization - reduces memory usage
      - --cache-type-k
      - "${CACHE_TYPE:-q8_0}"
      - --cache-type-v
      - "${CACHE_TYPE:-q8_0}"
      # Limit context checkpoint cache to 2GB
      - --cache-ram
      - "${CACHE_RAM:-2048}"
      # Metrics endpoint for monitoring
      - --metrics
    shm_size: 256m
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 180s
